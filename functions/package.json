{
  "name": "functions",
  "scripts": {
    "lint": "tslint --project tsconfig.json",
    "build": "tsc",
    "serve": "npm run build && firebase serve --only functions",
    "shell": "npm run build && firebase functions:shell",
    "start": "npm run shell",
    "deploy": "firebase deploy --only functions",
    "logs": "firebase functions:log",
    "test": "mocha --reporter spec",
    "test-online": "mocha --timeout 30000 --reporter spec test/test.online.js --exit"
  },
  "main": "lib/index.js",
  "dependencies": {
    "body-parser": "^1.18.3",
    "cookie-parser": "^1.4.3",
    "cors": "^2.8.4",
    "express": "^4.16.3",
    "firebase-admin": "~5.12.1",
    "firebase-functions": "^1.0.3",
    "moment": "^2.22.2",
    "request": "^2.86.0"
  },
  "devDependencies": {
    "chai": "^4.1.2",
    "chai-as-promised": "^7.1.1",
    "firebase-functions-test": "^0.1.1",
    "mocha": "^5.2.0",
    "sinon": "^6.0.0",
    "tslint": "^5.8.0",
    "typescript": "^2.5.3"
  },
  "private": true
}
